{
  "name": "cards-app",
  "version": "1.0.0",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "concurrently -k -n server,client \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  },
  "dependencies": {
    "react": "^16.7.0",
    "react-redux": "^6.0.0",
    "redux": "^4.0.1"
  }
}
